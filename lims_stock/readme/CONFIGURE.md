1. Install **LIMS Stock** (`lims_stock`) after `lims` and `stock`.
2. Open a product (*Inventory > Products*), enable **Is Specimen**.
3. (Optional) Give LIMS users access to stock lots if they need to open them.
